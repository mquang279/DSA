#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);cout.tie(NULL);
    int x, y;
    cin >> x >> y;
    cout << "Chu vi HCN la : " << 2 * (x + y) << endl;
    cout << "Dien tich HCN la : " << 1ll * x * y;
    return 0;
}