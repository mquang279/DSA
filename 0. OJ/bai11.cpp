#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);cout.tie(NULL);
    ll n;
    cin >> n;
    cout << n * 2 << endl << endl;
    cout << n * 10 << endl << endl;
    cout << n / 2 << endl << endl;
    cout << fixed << setprecision(3) << n / 2.00 << endl;
    return 0;
}