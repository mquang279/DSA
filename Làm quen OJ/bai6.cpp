#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    double x;
    cin >> x;
    cout << (int)floor(x) << endl << (int)ceil(x) << endl << (int)round(x);
    return 0;
}